apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - auth/
  - servarr/

secretGenerator:
  - name: homarr-oidc
    namespace: auth
    envs:
      - homarr_oidc.sops.env
  - name: homarr
    namespace: homarr
    envs:
      - homarr_oidc.sops.env
      - homarr_encryption_key.sops.env
