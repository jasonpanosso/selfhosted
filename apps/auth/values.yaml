pgcluster:
  name: pg-cluster
  instances: 3
  size: 1Gi
  storageClass: openebs-hostpath

autheliaDB:
  secretName: authelia-db-credentials
lldapDB:
  secretName: lldap-db-credentials

smtp:
  secretName: smtp

redis:
  secretName: redis

lldap:
  secretName: lldap

authelia:
  secretName: authelia
