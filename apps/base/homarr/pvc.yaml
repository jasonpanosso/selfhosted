---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homarr
  labels:
    app.kubernetes.io/name: homarr
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
