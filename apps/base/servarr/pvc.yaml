---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media
  namespace: servarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3500Gi
  storageClassName: rook-cephfs-hdd
