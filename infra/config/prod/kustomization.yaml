apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secrets/
  - imageupdateautomation.yaml
  - ../base

patches:
  - path: cephcluster-patch.yaml
    target:
      kind: CephCluster

configurations:
  - nameReference.yaml
