apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - flux-instance.yaml
secretGenerator:
  - name: cloudflare-api-token
    namespace: cert-manager
    files:
      - apiToken=cloudflare_token.enc
