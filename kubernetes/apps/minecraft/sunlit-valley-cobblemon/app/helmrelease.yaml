---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app sunlit-valley-cobblemon
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  timeout: 10m0s
  upgrade:
    strategy:
      name: RemediateOnFailure
    remediation:
      remediateLastFailure: true
      retries: 2
  values:
    fullnameOverride: *app
    replicaCount: 1
    image:
      repository: ghcr.io/itzg/minecraft-server
      tag: 2026.7.2-java21@sha256:7f69fd6688e03495c8a8f5a46e8a8e82001b4465f4b55bdcd024c02c3624d8c8
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: kubernetes.io/arch
                  operator: In
                  values:
                    - amd64
    resources:
      requests:
        cpu: 2000m
        memory: 8Gi
    podSecurityContext:
      runAsUser: 65534
      runAsGroup: 65534
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: [ALL]
    startupProbe:
      enabled: true
    extraEnv:
      TZ: America/Detroit
      USE_MEOWICE_FLAGS: "true"
    persistence:
      dataDir:
        enabled: true
        existingClaim: *app
    minecraftServer:
      eula: true
      version: 1.20.1
      type: AUTO_CURSEFORGE
      icon: https://cdn2.steamgriddb.com/logo_thumb/804a8294a8f33203683b3e6ed46fe092.png
      memory: 8G
      serviceType: LoadBalancer
      difficulty: normal
      levelType: normal
      spawnProtection: 0
      gameMode: survival
      pvp: default
      onlineMode: true
      whitelist: "Jayce"
      ops: "Jayce"
      motd: "GULP im dead lock in"
      viewDistance: default
      worldSaveName: sunlit-cobblemon
      rcon:
        enabled: true
        existingSecret: *app
        secretKey: MINECRAFT_COBBLEMON_RCON_PASSWORD
      autoCurseForge:
        apiKey:
          existingSecret: *app
          secretKey: CURSEFORGE_API_KEY
        slug: society-sunlit-cobblemon
        filenameMatcher: "1.0.5-SSV4.0.8"

    mcbackup:
      enabled: true
      persistence:
        backupDir:
          existingClaim: sunlit-valley-cobblemon-backup

    serviceAnnotations:
      lbipam.cilium.io/ips: 192.168.20.42
    deploymentAnnotations:
      reloader.stakater.com/auto: "true"
