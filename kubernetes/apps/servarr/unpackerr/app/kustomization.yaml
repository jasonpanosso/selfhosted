---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./deployment.yaml

configMapGenerator:
  - name: unpackerr
    files:
      - unpackerr.conf=./resources/unpackerr.toml

configurations:
  - ./resources/kustomizeconfig.yaml
