---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master-standalone/service-v1.json
apiVersion: v1
kind: Service
metadata:
  name: &app kube-controller-manager
  labels:
    app.kubernetes.io/name: *app
    jobLabel: *app
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    component: *app
  ports:
    - name: http-metrics
      port: 10257
      protocol: TCP
      targetPort: 10257
