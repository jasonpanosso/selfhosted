kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: shows
  namespace: servarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs
